{
  "buildCommand": "pnpm db:generate && pnpm build:server && pnpm build:client",
  "outputDirectory": "dist/client",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
