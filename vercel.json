{
  "buildCommand": "pnpm --filter @ai-cms/api db:generate && pnpm --filter @ai-cms/api build && pnpm --filter @ai-cms/web build",
  "outputDirectory": "apps/web/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
