# コーディングガイドライン原則（推奨）

言語に依存しないコーディングガイドラインの原則。

## 関数設計

- 1関数1責務
- 意味のない細分化をしない
- 意図しない副作用を避ける

## データ構造

- 異なる関心事のデータを同じコレクションに混在させない
- 意味が自明でない数値・文字列は命名された定数として定義する

## モジュール設計

- 循環参照を避け、依存関係を一方向に保つ

## 外部ライブラリ

- 内部実装（`_` プレフィックスのプロパティ等）へのアクセスは避け、公開 API を使用する
